Bootstrap: docker
From: rocker/r-ver:latest


%post

# Update packages
apt-get -y update
apt-get -y upgrade

# Install R packages
Rscript -e 'install.packages("pak")'
Rscript -e 'pak::pak("data.table")'

# Remove unneeded files
apt-get -y clean
rm -rf /var/lib/apt/lists/*
Rscript -e 'pak::pak_cleanup(force = TRUE)'


%test

Rscript --version


%runscript

R


%help

R with packages.
